// Task: 1

/*
* compare two number > & < 

*/

const a = 10;
const b = 11;

console.log(`${a} > ${b}`, a > b);
console.log(`${a} < ${b}`, b < a);

// Task: 2

/*
* compare two number >= & <= 

*/

console.log(`${a} >= ${b}`, a >= b);
console.log(`${a} <= ${b}`, b <= a);

// Task: 3

/*
* compare two number == & === 

*/

const first = 1;
const second = "1";

console.log(`${first} == ${second}`, first == second);
console.log(`${first} === ${second}`, first === second);
